# Copy to .env and fill in real values. Never commit .env.

# Supabase Transaction Pooler URL (Project Settings > Database > Connection string)
DATABASE_URL=postgresql://postgres.xxxxx:password@aws-x-us-east-1.pooler.supabase.com:6543/postgres

# Shared with sync-server — MUST be the same value in both .env files
JWT_SECRET=replace-with-a-long-random-string
